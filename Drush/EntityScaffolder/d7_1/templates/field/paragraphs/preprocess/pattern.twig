
  {{ map }} ({{ field_name }})
    type            : paragrpah reference
    cardinality     : {{ cardinality }}
    variable type   : Drupal render array
    Note : Use this value directly as `{{ map }} | raw`
