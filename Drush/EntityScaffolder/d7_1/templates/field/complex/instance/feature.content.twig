

  // Exported field_instance: '{{ entity_type }}-{{ bundle }}-{{ field_name }}'.
  $field_instances['{{ entity_type }}-{{ bundle }}-{{ field_name }}'] = array(
    'bundle' => '{{ bundle }}',
    'default_value' => NULL,
    'deleted' => 0,
    'description' => '{{ description | raw }}',
    'display' => array(
      'default' => array(
        'label' => 'hidden',
        'module' => 'paragraphs',
        'settings' => array(
          'view_mode' => 'full',
        ),
        'type' => 'paragraphs_view',
        'weight' => 0,
      ),
    ),
    'entity_type' => '{{ entity_type }}',
    'field_name' => '{{ field_name }}',
    'label' => '{{ label }}',
    'required' => {{ required }},
    'settings' => array(
      'add_mode' => 'button',
      'allowed_bundles' => array(
{% for allowed_bundle in allowed_bundles %}
        '{{ allowed_bundle }}' => '{{ allowed_bundle }}',
{% endfor %}
      ),
      'bundle_weights' => array(
      ),
      'default_edit_mode' => {% if default_edit_mode is empty %}'closed'{% else %}{{ default_edit_mode }}{% endif %},
      'title' => '{{ label }}',
      'title_multiple' => '{{ label }}s',
      'user_register_form' => FALSE,
    ),
    'widget' => array(
      'active' => 0,
      'module' => 'paragraphs',
      'settings' => array(),
      'type' => 'paragraphs_embed',
      'weight' => {{ weight }},
    ),
  );
