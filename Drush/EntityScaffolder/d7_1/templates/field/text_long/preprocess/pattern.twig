
  {{ map }} ({{ field_name }})
    type            : text_long
    cardinality     : {{ cardinality }}
    variable type   : text
{% if max_length is not empty %}
    character limit : {{ max_length }}
{% endif %}
