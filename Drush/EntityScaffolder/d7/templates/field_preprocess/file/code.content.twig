
  // Preprocess {{ field_name }}.
  $file = NULL;
  if (!empty($vars['{{ field_name }}'])) {
    foreach ($vars['{{ field_name }}'] as $file) {
      $file = es_helper_get_file_data($file);
    }
  }
  $vars['{{ map }}'] = $file;
