{% if cardinality == 1 %}
  // Preprocess {{ field_name }}.
  $vars['{{ map }}'] = $vars['content']['{{ field_name }}'];
{% else %}
  // Preprocess {{ field_name }}.
  $vars['{{ map }}'] = $vars['content']['{{ field_name }}'];
{% endif %}
